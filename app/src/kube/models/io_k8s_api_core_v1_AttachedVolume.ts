

/**
 * AttachedVolume describes a volume attached to a node
 */
export type io_k8s_api_core_v1_AttachedVolume = {
	/**
	 * DevicePath represents the device path where the volume should be available
	 */
	devicePath: string;
	/**
	 * Name of the attached volume
	 */
	name: string;
};

