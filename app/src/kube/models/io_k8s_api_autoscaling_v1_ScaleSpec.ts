

/**
 * ScaleSpec describes the attributes of a scale subresource.
 */
export type io_k8s_api_autoscaling_v1_ScaleSpec = {
	/**
	 * replicas is the desired number of instances for the scaled object.
	 */
	replicas?: number;
};

