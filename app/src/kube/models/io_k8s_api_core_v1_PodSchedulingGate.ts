

/**
 * PodSchedulingGate is associated to a Pod to guard its scheduling.
 */
export type io_k8s_api_core_v1_PodSchedulingGate = {
	/**
	 * Name of the scheduling gate. Each scheduling gate must have a unique name field.
	 */
	name: string;
};

